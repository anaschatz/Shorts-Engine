import assert from "node:assert/strict";
import { readFileSync } from "node:fs";

const html = readFileSync("index.html", "utf8");
const app = readFileSync("app.js", "utf8");
const hardening = readFileSync("hardening.js", "utf8");
const serverApp = readFileSync("server/app.cjs", "utf8");
const youtubeIngest = readFileSync("server/youtube-ingest.cjs", "utf8");
const youtubeDownloaderErrors = readFileSync("server/youtube-downloader-errors.cjs", "utf8");
const localYoutubeAdapter = readFileSync("server/adapters/local-youtube-ingest-adapter.cjs", "utf8");
const mockYoutubeAdapter = readFileSync("server/adapters/mock-youtube-ingest-adapter.cjs", "utf8");
const youtubeIngestAdapter = readFileSync("server/adapters/youtube-ingest-adapter.cjs", "utf8");
const youtubeIngestService = readFileSync("server/youtube-ingest-service.cjs", "utf8");
const css = readFileSync("styles.css", "utf8");
const fixtureScript = readFileSync("demo/create-fixture.mjs", "utf8");
const browserSmoke = readFileSync("demo/run-browser-smoke.mjs", "utf8");
const playwrightSmoke = readFileSync("demo/run-playwright-smoke.mjs", "utf8");
const demoSmoke = readFileSync("demo/run-smoke.mjs", "utf8");
const ocrSmoke = readFileSync("demo/run-ocr-smoke.mjs", "utf8");
const ocrQaReview = readFileSync("demo/ocr-qa-review.mjs", "utf8");
const ocrQaReviewRunner = readFileSync("demo/run-ocr-qa-review.mjs", "utf8");
const ocrQaCalibration = readFileSync("server/ocr-qa-calibration.cjs", "utf8");
const youtubeSmoke = readFileSync("demo/run-youtube-smoke.mjs", "utf8");
const youtubeLiveE2E = readFileSync("demo/run-youtube-live-e2e.mjs", "utf8");
const humanVisualReview = readFileSync("demo/run-human-visual-review.mjs", "utf8");
const reportSafety = readFileSync("demo/report-safety.mjs", "utf8");
const ciReportValidator = readFileSync("demo/validate-ci-reports.mjs", "utf8");
const envChecker = readFileSync("tools/release/check-environment.mjs", "utf8");
const ocrDoctor = readFileSync("tools/release/check-ocr-runtime.mjs", "utf8");
const youtubeDoctor = readFileSync("tools/release/check-youtube-ingest.mjs", "utf8");
const stagingReadiness = readFileSync("tools/release/check-staging-readiness.mjs", "utf8");
const stagingSmoke = readFileSync("tools/release/check-staging-smoke.mjs", "utf8");
const releaseReadiness = readFileSync("server/release-readiness.cjs", "utf8");
const releaseReadinessCheck = readFileSync("tools/release/check-release-readiness.mjs", "utf8");
const renderCheck = readFileSync("tools/release/check-render-staging.mjs", "utf8");
const renderManual = readFileSync("tools/release/print-render-staging-checklist.mjs", "utf8");
const renderProof = readFileSync("tools/release/render-staging-proof.mjs", "utf8");
const stagingDeploy = readFileSync("tools/release/staging-deploy.mjs", "utf8");
const githubDoctor = readFileSync("tools/release/check-github-cli.mjs", "utf8");
const githubSetup = readFileSync("tools/release/print-github-cli-setup.mjs", "utf8");
const remoteCi = readFileSync("tools/release/check-remote-ci.mjs", "utf8");
const remoteCiProof = readFileSync("tools/release/write-remote-ci-proof.mjs", "utf8");
const branchPolicy = readFileSync("tools/release/check-branch-protection.mjs", "utf8");
const branchPolicyProof = readFileSync("tools/release/write-branch-protection-proof.mjs", "utf8");
const branchSetup = readFileSync("tools/release/print-branch-ruleset-setup.mjs", "utf8");
const stagingFullSmoke = readFileSync("tools/release/check-staging-full-smoke.mjs", "utf8");
const stagingFullCleanup = readFileSync("tools/release/cleanup-staging-full-smoke.mjs", "utf8");
const releaseGateVerifier = readFileSync("tools/release/verify-release-gate.mjs", "utf8");
const releaseEvidenceWriter = readFileSync("tools/release/write-release-evidence.mjs", "utf8");
const envDocs = readFileSync("docs/ENVIRONMENT.md", "utf8");
const youtubeManualDocs = readFileSync("docs/YOUTUBE_INGEST_MANUAL_SMOKE.md", "utf8");
const envExample = readFileSync(".env.example", "utf8");
const stagingDocs = readFileSync("docs/STAGING_DEPLOYMENT.md", "utf8");
const manualDocs = readFileSync("demo/MANUAL_TESTING.md", "utf8");
const ciDocs = readFileSync("demo/CI.md", "utf8");
const releaseDocs = readFileSync("docs/RELEASE.md", "utf8");
const githubWorkflow = readFileSync(".github/workflows/ci.yml", "utf8");
const stagingWorkflow = readFileSync(".github/workflows/staging.yml", "utf8");
const packageJson = readFileSync("package.json", "utf8");
const gitignore = readFileSync(".gitignore", "utf8");

assert.match(html, /Content-Security-Policy/, "index.html should include a CSP meta tag");
assert.match(html, /ShortsEngine/, "index.html should use the current user-facing product name");
assert.match(html, /hardening\.js/, "index.html should load the hardening layer before app.js");
assert.match(html, /id="rightsCheckbox"/, "rights consent checkbox must be explicit");
assert.match(html, /id="errorPanel"/, "global error panel must exist");
assert.match(html, /id="downloadLink"[^>]*hidden/, "download link should start hidden");
assert.match(html, /data-testid="video-upload-input"/, "upload input needs a stable browser test selector");
assert.match(html, /data-testid="source-local-button"/, "local source selector needs a stable browser test selector");
assert.match(html, /data-testid="source-youtube-button"/, "YouTube source selector needs a stable browser test selector");
assert.match(html, /data-testid="youtube-url-input"/, "YouTube URL input needs a stable browser test selector");
assert.match(html, /data-testid="youtube-rights-checkbox"/, "YouTube rights checkbox needs a stable browser test selector");
assert.match(html, /data-testid="youtube-validate-button"/, "YouTube validate button needs a stable browser test selector");
assert.match(html, /data-testid="youtube-ingest-button"[^>]*disabled/, "YouTube ingest action should start disabled");
assert.match(html, /data-testid="youtube-ingest-status"/, "YouTube ingest status needs a stable browser test selector");
assert.match(html, /data-testid="youtube-preview"/, "YouTube validation preview needs a stable browser test selector");
assert.match(html, /data-testid="youtube-warning"/, "YouTube ingest warning needs a stable browser test selector");
assert.match(html, /data-testid="youtube-recovery-actions"/, "YouTube ingest recovery actions need a stable browser test selector");
assert.match(html, /data-testid="youtube-retry-ingest-button"/, "YouTube retry ingest needs a stable browser test selector");
assert.match(html, /data-testid="youtube-use-another-button"/, "YouTube replace-link recovery needs a stable browser test selector");
assert.match(html, /data-testid="youtube-upload-mp4-fallback-button"/, "YouTube MP4 fallback recovery needs a stable browser test selector");
assert.match(html, /data-testid="rights-checkbox"/, "rights checkbox needs a stable browser test selector");
assert.match(html, /data-testid="generate-button"/, "generate button needs a stable browser test selector");
assert.match(html, /data-testid="cancel-job-button"/, "cancel button needs a stable browser test selector");
assert.match(html, /data-testid="export-button"[^>]*disabled/, "export button should start disabled and selectable");
assert.match(html, /data-testid="download-link"[^>]*hidden/, "download link should start hidden and selectable");
assert.match(html, /data-testid="error-panel"/, "error panel needs a stable browser test selector");
assert.match(html, /data-testid="job-progress-bar"/, "progress bar needs a stable browser test selector");
assert.match(html, /data-testid="project-status"/, "project status needs a stable browser test selector");
assert.match(html, /data-export-target="tiktok" disabled/, "export targets should start disabled");
assert.match(html, /accept="\.mp4,\.mov,\.webm,video\/mp4,video\/quicktime,video\/webm"/);

assert.doesNotMatch(app, /\.innerHTML\s*=/, "app.js should not assign raw innerHTML");
assert.doesNotMatch(app, /insertAdjacentHTML/, "app.js should not inject HTML strings");
assert.match(app, /validateUploadFile/, "app.js should validate uploads before preview");
assert.match(app, /validateVideoSignature/, "app.js should inspect media signatures");
assert.match(app, /validateVideoDuration/, "app.js should validate media duration");
assert.match(app, /createIdempotencyKey/, "app.js should use idempotency keys");
assert.match(app, /createRateLimiter/, "app.js should use rate limits");
assert.match(app, /function resetRenderState/, "app.js should clear stale render state on new uploads");
assert.match(app, /function formatJobStep/, "app.js should show friendly analysis progress steps");
assert.match(app, /momentsFromCandidatePlans/, "app.js should render generated candidate moments");
assert.match(app, /validateCompletedJobForExport/, "app.js should validate completed jobs before enabling downloads");
assert.match(app, /EXPORT_NOT_READY/, "app.js should fail closed when download/export is not ready");
assert.match(app, /validateYouTubeSourceInput/, "app.js should validate YouTube URLs before API calls");
assert.match(app, /deriveYouTubeUiState/, "app.js should derive YouTube control state from a shared helper");
assert.match(app, /scheduleYouTubeAutoValidate/, "YouTube validation should run automatically after URL and rights are ready");
assert.match(app, /validateYoutubeBtn\.hidden = !showValidationFallback/, "YouTube validation button should be retry/loading feedback, not the primary flow");
assert.match(app, /validateYoutubeBtn\.textContent = state\.youtubeAction === "validating" \? "Validating\.\.\." : "Retry validation"/, "YouTube validation fallback should clearly communicate retry behavior");
assert.match(app, /ingestYoutubeBtn\.disabled = !youtubeUi\.canIngest/, "YouTube ingest should be disabled until validation and health readiness pass");
assert.match(app, /generateBtn\.disabled = !youtubeUi\.canGenerate/, "YouTube generate should remain disabled until ingest creates project/upload state");
assert.match(app, /createYouTubePreviewSummary/, "YouTube preview should use a safe summary helper");
assert.match(app, /youtubePreviewUrl\.textContent = summary\.label/, "YouTube preview should show a safe source label instead of raw canonical URL");
assert.match(app, /autoFillMatchTitle/, "app.js should auto-fill match title from safe source metadata when available");
assert.match(app, /state\.titleEdited = true/, "app.js should preserve user-edited match titles");
assert.match(hardening, /createProjectTitleCandidate/, "hardening.js should expose a safe project title candidate helper");
assert.match(app, /\/api\/youtube\/validate/, "app.js should use the validate-only YouTube source endpoint");
assert.match(app, /\/api\/youtube\/ingest/, "app.js should call the dedicated YouTube ingest endpoint only after validation");
assert.match(app, /youtubeHealth/, "app.js should gate YouTube ingest on backend health");
assert.match(app, /ingestYoutubeBtn\.disabled/, "app.js should keep YouTube ingest disabled until validation and readiness pass");
assert.match(app, /YOUTUBE_INGEST_NOT_ENABLED/, "app.js should fail closed before YouTube ingest creates an upload");
assert.match(app, /state\.sourceType === "youtube"/, "app.js should branch YouTube source state explicitly");
assert.doesNotMatch(app, /yt-dlp|youtube-dl|execFile|spawn|child_process|stdout|stderr|storageKey|document\.cookie/i, "frontend must not contain downloader logic or unsafe internal fields");

assert.match(serverApp, /createLocalJobWorker/, "server should use the durable local worker abstraction");
assert.match(serverApp, /createLocalJobQueue/, "server should use the queue contract adapter boundary");
assert.match(serverApp, /createWorkerSupervisor/, "server should use the worker supervisor abstraction");
assert.match(serverApp, /jobQueue\.enqueue/, "server routes should enqueue through the queue contract boundary");
assert.match(serverApp, /workerSupervisor\.enqueue/, "server routes should delegate render orchestration through the worker supervisor");
assert.doesNotMatch(serverApp, /function renderPipeline|async function renderPipeline/, "server/app.cjs should not own the render pipeline");
assert.match(serverApp, /handleYouTubeValidate/, "server should expose a thin YouTube validation route");
assert.match(serverApp, /handleYouTubeIngest/, "server should expose a thin YouTube ingest route");
assert.match(serverApp, /validateYouTubeSource/, "server route should delegate YouTube validation to domain logic");
assert.match(serverApp, /createYouTubeIngestService/, "server route should delegate YouTube ingest orchestration to a service");
assert.match(serverApp, /youtubeIngestHealth/, "health should include YouTube ingest readiness");
assert.match(serverApp, /createGoalEvidenceProvider/, "health should include the goal evidence provider boundary");
assert.match(serverApp, /goalEvidence/, "health should report safe goal evidence readiness");
assert.match(serverApp, /scoreboardOcrHealth/, "health should include scoreboard OCR readiness");
assert.match(serverApp, /scoreboardOcr/, "health should report safe scoreboard OCR readiness");
assert.match(youtubeIngest, /YOUTUBE_PLAYLIST_UNSUPPORTED/, "YouTube validation should reject playlists explicitly");
assert.match(youtubeIngest, /YOUTUBE_LIVE_UNSUPPORTED/, "YouTube validation should reject live streams explicitly");
assert.match(youtubeIngest, /YOUTUBE_INGEST_NOT_ENABLED/, "YouTube adapter failures should fail closed with a safe code");
assert.match(youtubeIngest, /readAdapterMetadata/, "YouTube metadata adapter reads should stay behind a small defensive boundary");
assert.match(youtubeIngest, /canonicalUrl/, "YouTube validation should return a canonical URL");
assert.match(html, /id="youtubeError"[^>]*role="alert"/, "YouTube field errors should be announced accessibly");
assert.match(app, /clearYouTubeFieldError/, "YouTube field errors should clear when the user edits the source");
assert.doesNotMatch(youtubeIngest, /\bfetch\s*\(|child_process|spawn|execFile|yt-dlp|youtube-dl|https\.request|http\.request/i, "YouTube domain validation must not download, call network APIs or spawn tools");
assert.match(youtubeIngestAdapter, /createMockYouTubeIngestAdapter/, "YouTube adapter factory should keep mock as the default boundary");
assert.match(youtubeIngestAdapter, /createLocalYouTubeIngestAdapter/, "YouTube adapter factory should enable a local adapter only through config");
assert.match(localYoutubeAdapter, /execFile/, "local YouTube adapter should use execFile instead of shell commands");
assert.match(localYoutubeAdapter, /--no-playlist/, "local YouTube adapter should reject playlists at downloader invocation too");
assert.match(localYoutubeAdapter, /maxBuffer/, "local YouTube adapter should bound downloader output");
assert.match(localYoutubeAdapter, /timeout/, "local YouTube adapter should bound downloader runtime");
assert.match(localYoutubeAdapter, /assertStoragePath\(outputPath,\s*"staging"\)/, "local YouTube adapter should restrict output to staging");
assert.match(localYoutubeAdapter, /YOUTUBE_DOWNLOADER_MISSING/, "local YouTube adapter should fail safely when the downloader is unavailable");
assert.match(localYoutubeAdapter, /toSafeYouTubeDownloaderError/, "local YouTube adapter should delegate downloader failures to the safe classifier");
assert.match(youtubeDownloaderErrors, /YOUTUBE_DOWNLOAD_TIMEOUT/, "YouTube downloader classifier should report timeouts with a safe code");
assert.doesNotMatch(localYoutubeAdapter, /\bexec\s*\(|spawn\s*\([^S]|shell:\s*true|youtube-dl/i, "local YouTube adapter must not use shell execution or youtube-dl");
assert.match(youtubeIngestService, /validateYouTubeSource/, "YouTube ingest service should reuse domain URL and rights validation");
assert.match(youtubeIngestService, /streamLocalPathToArtifact/, "YouTube ingest service should commit validated staging output through artifact store");
assert.match(youtubeIngestService, /validateUploadCandidate/, "YouTube ingest service should reuse upload media validation");
assert.match(youtubeIngestService, /probeMedia/, "YouTube ingest service should FFprobe downloaded media before creating records");
assert.match(youtubeIngestService, /createProjectUpload/, "YouTube ingest service should create project/upload records only after artifact creation");
assert.match(youtubeIngestService, /cleanupYouTubeStage/, "YouTube ingest service should clean failed staging files");
assert.doesNotMatch(youtubeIngestService, /storageKey[^,\n]*sendOk|outputPath[^,\n]*sendOk|filePath[^,\n]*sendOk/, "YouTube ingest service should not expose storage internals in public responses");
assert.match(mockYoutubeAdapter, /networkCalls:\s*false/, "mock YouTube adapter must declare no network calls");
assert.match(mockYoutubeAdapter, /downloaderConfigured:\s*false/, "mock YouTube adapter must keep downloader disabled");
assert.doesNotMatch(mockYoutubeAdapter, /\bfetch\s*\(|child_process|spawn|execFile|yt-dlp|youtube-dl/i, "mock YouTube adapter must not download or spawn tools");

assert.match(fixtureScript, /testsrc2/, "demo fixture should be generated deterministically");
assert.match(fixtureScript, /sine=frequency=880/, "demo fixture should include a simple audio track for transcription fallback coverage");
assert.match(demoSmoke, /demo\/results\/latest\.json|RESULTS_DIR/, "demo smoke should write a report");
assert.match(demoSmoke, /invalid_upload_rejected/, "demo smoke should verify invalid upload rejection");
assert.match(demoSmoke, /download_returns_rendered_video/, "demo smoke should verify rendered export download");
assert.match(demoSmoke, /hasSensitiveLeak/, "demo smoke should guard public outputs against leaks");
assert.match(demoSmoke, /findSensitiveLeak/, "demo smoke should report safe leak metadata when fail-closed");
assert.match(youtubeDoctor, /SHORTSENGINE_YOUTUBE_INGEST_ENABLED/, "YouTube doctor should check the explicit ingest flag");
assert.match(youtubeDoctor, /YOUTUBE_DOWNLOADER_MISSING/, "YouTube doctor should fail safely when the downloader is missing");
assert.match(youtubeDoctor, /commandAvailable/, "YouTube doctor should validate FFmpeg and FFprobe availability");
assert.match(youtubeDoctor, /storageHealth/, "YouTube doctor should validate local storage readiness");
assert.match(youtubeDoctor, /validateHealthYoutubeShape/, "YouTube doctor should validate live health youtubeIngest shape when configured");
assert.match(youtubeDoctor, /findSensitiveLeak/, "YouTube doctor should guard summaries and health responses against leaks");
assert.match(youtubeDoctor, /DOCTOR_NEXT_ACTIONS/, "YouTube doctor should centralize safe operator next actions");
assert.match(youtubeDoctor, /install-configure-downloader-or-set-SHORTSENGINE_YOUTUBE_DOWNLOADER_BIN/, "YouTube doctor should guide missing downloader failures safely");
assert.match(youtubeDoctor, /fix-live-health-youtubeIngest-shape-or-use-local-doctor/, "YouTube doctor should guide invalid live health shape safely");
assert.match(ocrDoctor, /scoreboardConfig/, "OCR doctor should inspect the configured scoreboard OCR provider");
assert.match(ocrDoctor, /ocrCommandAvailable/, "OCR doctor should check the OCR command through the local adapter helper");
assert.match(ocrDoctor, /storageHealth/, "OCR doctor should validate local staging storage readiness");
assert.match(ocrDoctor, /frameExtractionHealth/, "OCR doctor should include frame extraction readiness");
assert.match(ocrDoctor, /findSensitiveLeak/, "OCR doctor should guard readiness output against leaks");
assert.doesNotMatch(ocrDoctor, /auth login|npm install|brew install|curl\s+|gh secret|stdout|stderr/i, "OCR doctor must not install tools, start auth or expose command output");
assert.match(ocrSmoke, /ocr-latest\.json/, "OCR smoke should write a stable latest report");
assert.match(ocrSmoke, /extractSampledFrames/, "OCR smoke should use the frame extraction boundary");
assert.match(ocrSmoke, /analyzeScoreboardOcr/, "OCR smoke should use the scoreboard OCR provider boundary");
assert.match(ocrSmoke, /publicScoreboardOcr/, "OCR smoke should persist only public OCR evidence");
assert.match(ocrSmoke, /findSensitiveLeak/, "OCR smoke should guard reports against leaks");
assert.match(ocrSmoke, /ocrTextStored:\s*false/, "OCR smoke reports should not store OCR text");
assert.match(ocrSmoke, /SHORTSENGINE_OCR_QA_ARTIFACTS/, "OCR smoke crop artifacts should require explicit opt-in env");
assert.match(ocrSmoke, /cleanupOcrQaArtifacts/, "OCR smoke should clean up old managed OCR QA artifacts");
assert.match(ocrSmoke, /OCR_ARTIFACTS_RELATIVE_DIR/, "OCR smoke should keep artifact refs under the managed OCR artifacts directory");
assert.match(ocrSmoke, /OCR_QA_ARTIFACT_MANIFEST_FILE/, "OCR smoke should write a bounded manifest for opt-in OCR QA artifacts");
assert.match(ocrSmoke, /validateOcrQaArtifactRecord/, "OCR smoke should validate crop artifact records before report exposure");
assert.doesNotMatch(ocrSmoke, /auth login|npm install|brew install|curl\s+|rawOcr|rawText|stdoutIncluded|stderrIncluded/i, "OCR smoke must not install tools or persist raw OCR/debug fields");
assert.match(packageJson, /"ocr:qa:review"\s*:\s*"node demo\/run-ocr-qa-review\.mjs"/, "package scripts should expose the OCR QA review runner");
assert.match(ocrQaReview, /OCR_QA_REVIEW_LATEST_RELATIVE_PATH/, "OCR QA review should write a stable latest report");
assert.match(ocrQaReview, /validateManifestRelativeRef/, "OCR QA review should validate manifest refs before reading");
assert.match(ocrQaReview, /goalEvidencePolicy:\s*"support_only"/, "OCR QA review calibration should remain support-only");
assert.match(ocrQaReview, /goalDecisionAllowed:\s*false/, "OCR QA review must not allow OCR-only goal decisions");
assert.match(ocrQaReview, /noFalseGoalFromOcrOnly:\s*true/, "OCR QA review should preserve no-false-goal calibration");
assert.match(ocrQaReview, /findSensitiveLeak/, "OCR QA review should guard reports against leaks");
assert.match(ocrQaReview, /ocrTextStored:\s*false/, "OCR QA review reports should not store OCR text");
assert.match(ocrQaReview, /fullFramesStored:\s*false/, "OCR QA review reports should not store full frames");
assert.match(ocrQaReview, /logsDownloaded:\s*false/, "OCR QA review should not download logs");
assert.match(ocrQaReview, /artifactsDownloaded:\s*false/, "OCR QA review should not download remote artifacts");
assert.match(ocrQaReview, /MAX_REVIEW_CROPS/, "OCR QA review should bound manual crop review size");
assert.match(ocrQaCalibration, /OCR_QA_REVIEW_LATEST_RELATIVE_PATH/, "OCR QA calibration should read the stable review report path");
assert.match(ocrQaCalibration, /DEFAULT_MAX_AGE_MS/, "OCR QA calibration should reject stale review reports");
assert.match(ocrQaCalibration, /goalEvidencePolicy:\s*"support_only"/, "OCR QA calibration must remain support-only");
assert.match(ocrQaCalibration, /goalDecisionAllowed:\s*false/, "OCR QA calibration must not allow OCR-only goal decisions");
assert.match(ocrQaCalibration, /noFalseGoalFromOcrOnly:\s*true/, "OCR QA calibration should preserve no-false-goal policy");
assert.match(ocrQaCalibration, /hasSensitiveLeak/, "OCR QA calibration should reject leaking reports");
assert.match(ocrQaCalibration, /safeRootRelativePath/, "OCR QA calibration should reject unsafe report refs");
assert.doesNotMatch(ocrQaCalibration, /rawOcrText|stdoutIncluded|stderrIncluded|auth login|npm install|brew install|curl\s+/i, "OCR QA calibration must not consume raw OCR, install tools or auth");
assert.match(ocrQaReviewRunner, /SHORTSENGINE_OCR_QA_REVIEW_INPUT/, "OCR QA review CLI should support explicit operator input");
assert.doesNotMatch(ocrQaReview, /auth login|npm install|brew install|curl\s+|tesseract --version|gh secret|rawOcrText|stdoutIncluded|stderrIncluded/i, "OCR QA review must not install tools, start auth or persist raw OCR/debug fields");
assert.doesNotMatch(ocrQaReviewRunner, /auth login|npm install|brew install|curl\s+|tesseract --version|gh secret|rawOcrText|stdoutIncluded|stderrIncluded/i, "OCR QA review runner must stay documentation/input-only and safe");
assert.match(youtubeSmoke, /SHORTSENGINE_YOUTUBE_SMOKE/, "YouTube smoke should require an explicit smoke flag");
assert.match(youtubeSmoke, /SHORTSENGINE_YOUTUBE_SMOKE_URL/, "YouTube smoke should read the authorized test URL from env");
assert.match(youtubeSmoke, /SHORTSENGINE_YOUTUBE_SMOKE_ALLOW_UNLISTED/, "YouTube smoke should require allowlist or explicit manual unlisted flag");
assert.match(youtubeSmoke, /normalizeYouTubeUrl/, "YouTube smoke should reject unsafe YouTube URL forms before network calls");
assert.match(youtubeSmoke, /\/api\/youtube\/validate/, "YouTube smoke should validate the source before ingest");
assert.match(youtubeSmoke, /\/api\/youtube\/ingest/, "YouTube smoke should call the dedicated ingest endpoint");
assert.match(youtubeSmoke, /\/api\/projects\/\$\{ingested\.projectId\}\/generate/, "YouTube smoke should start the render job after ingest");
assert.match(youtubeSmoke, /\/api\/exports\/\$\{ids\.exportId\}\/download/, "YouTube smoke should download the completed export");
assert.match(youtubeSmoke, /ftyp/, "YouTube smoke should validate MP4 signatures");
assert.match(youtubeSmoke, /findSensitiveLeak/, "YouTube smoke should guard reports and public responses against leaks");
assert.match(youtubeSmoke, /youtube-smoke-latest\.json/, "YouTube smoke should write a stable latest report");
assert.match(youtubeSmoke, /YOUTUBE_SMOKE_HEALTH_NOT_READY/, "YouTube smoke should fail closed when live health is not ready");
assert.match(youtubeSmoke, /requestIdPresent/, "YouTube smoke reports should keep request id presence instead of raw ids");
assert.match(youtubeSmoke, /SMOKE_NEXT_ACTIONS/, "YouTube smoke should centralize safe operator next actions");
assert.doesNotMatch(youtubeSmoke, /yt-dlp|youtube-dl|execFile|spawn|child_process/, "YouTube smoke runner should not invoke downloader tools directly");
assert.match(youtubeLiveE2E, /SHORTSENGINE_YOUTUBE_LIVE_E2E/, "YouTube live E2E should require an explicit live proof flag");
assert.match(youtubeLiveE2E, /SHORTSENGINE_YOUTUBE_LIVE_E2E_RIGHTS_CONFIRMED/, "YouTube live E2E should require explicit rights confirmation");
assert.match(youtubeLiveE2E, /checkEnvironment/, "YouTube live E2E should run env readiness before doctor/server work");
assert.match(youtubeLiveE2E, /command:\s*commandName/, "YouTube live E2E reports should include the operator command");
assert.match(youtubeLiveE2E, /passed:\s*status === "passed"/, "YouTube live E2E reports should include a passed boolean");
assert.match(youtubeLiveE2E, /skipped:\s*status === "skipped"/, "YouTube live E2E reports should include a skipped boolean");
assert.match(youtubeLiveE2E, /phaseForCode/, "YouTube live E2E should classify failures by triage phase");
assert.match(youtubeLiveE2E, /safePreflightSummary/, "YouTube live E2E should include a safe preflight triage summary");
assert.match(youtubeLiveE2E, /checkYouTubeIngest/, "YouTube live E2E should run the doctor before server smoke work");
assert.match(youtubeLiveE2E, /runYouTubeSmoke/, "YouTube live E2E should reuse the safe smoke pipeline");
assert.match(youtubeLiveE2E, /youtube-live-e2e-latest\.json/, "YouTube live E2E should write a stable latest report");
assert.match(youtubeLiveE2E, /YOUTUBE_LIVE_E2E_SERVER_BIND_FAILED/, "YouTube live E2E should classify server bind failures safely");
assert.doesNotMatch(youtubeLiveE2E, /yt-dlp|youtube-dl|execFile|shell:\s*true|storageKey/, "YouTube live E2E should not execute downloader tools or persist unsafe internals");
assert.match(humanVisualReview, /human-visual-review-latest\.json/, "human visual review should write a stable latest report");
assert.match(humanVisualReview, /generatedArtifactFromProof/, "human visual review should extract generated video artifacts from live proof");
assert.match(humanVisualReview, /pending_human_review/, "human visual review should distinguish machine pass from pending human product review");
assert.match(humanVisualReview, /HUMAN_VISUAL_CHECKLIST/, "human visual review should keep a stable operator checklist");
assert.match(humanVisualReview, /findSensitiveLeak/, "human visual review should guard reports against leaks");
assert.match(humanVisualReview, /loadLatestHumanVisualReviewReport/, "human visual review should expose latest report loading for the UI/API");
assert.match(humanVisualReview, /writeHumanVisualReviewFromPayload/, "human visual review should expose a safe API writer");
assert.match(humanVisualReview, /normalizeApiReviewPayload/, "human visual review API payloads should be normalized before scoring");
assert.match(humanVisualReview, /logsDownloaded: false/, "human visual review should not download raw logs");
assert.match(humanVisualReview, /artifactsDownloaded: false/, "human visual review should not download remote artifacts");
assert.doesNotMatch(humanVisualReview, /rawLogsIncluded/, "human visual review reports should avoid unsafe raw log field names");
assert.match(serverApp, /handleHumanReviewLatest/, "server should expose a thin human review latest handler");
assert.match(serverApp, /handleHumanReviewSubmit/, "server should expose a thin human review submit handler");
assert.match(serverApp, /safeReviewMediaRef/, "server should validate human review media refs through a dedicated helper");
assert.match(serverApp, /publicHumanReviewMetrics/, "server should allowlist public human review metrics");
assert.match(serverApp, /publicHumanReviewFlags/, "server should allowlist public human review flags");
assert.match(serverApp, /publicHumanReviewCriterion/, "server should sanitize public human review criteria");
assert.match(serverApp, /\/api\/review\/latest/, "server should expose human review latest API");
assert.match(serverApp, /\/api\/review\/human/, "server should expose human review submit API");
assert.match(serverApp, /\/api\/review\/media/, "server should expose safe review media previews");
assert.match(serverApp, /handleOcrQaLatest/, "server should expose a thin OCR QA latest handler");
assert.match(serverApp, /handleOcrQaReviewSubmit/, "server should expose a thin OCR QA review submit handler");
assert.match(serverApp, /handleOcrQaCrop/, "server should expose validated OCR QA crop thumbnails");
assert.match(serverApp, /\/api\/ocr-qa\/latest/, "server should expose OCR QA latest API");
assert.match(serverApp, /\/api\/ocr-qa\/review/, "server should expose OCR QA review submit API");
assert.match(serverApp, /\/api\/ocr-qa\/crop/, "server should expose OCR QA crop thumbnail API");
assert.match(serverApp, /OCR_QA_SUPPORT_POLICY/, "server should keep OCR evidence support-only");
assert.match(serverApp, /manual-downloads\//, "review media streaming should be limited to manual-downloads MP4 refs");
assert.match(html, /data-testid="human-review-form"/, "human review form needs a stable browser selector");
assert.match(html, /data-testid="human-review-generated-ref"/, "human review generated ref needs a stable browser selector");
assert.match(html, /data-testid="human-review-reference-ref"/, "human review reference ref needs a stable browser selector");
assert.match(html, /data-testid="human-review-submit-button"/, "human review submit action needs a stable browser selector");
assert.match(html, /data-testid="human-review-status"/, "human review status needs a stable browser selector");
assert.match(html, /data-testid="ocr-qa-panel"/, "OCR QA panel needs a stable browser selector");
assert.match(html, /data-testid="ocr-qa-crops"/, "OCR QA crop list needs a stable browser selector");
assert.match(html, /data-testid="ocr-qa-submit-button"[^>]*disabled/, "OCR QA submit should start disabled");
assert.match(html, /data-testid="ocr-qa-calibration"/, "OCR QA calibration output needs a stable browser selector");
assert.match(app, /renderHumanReviewControls/, "app should render the human review scoring controls from the shared contract");
assert.match(app, /validateHumanReviewForm/, "app should validate human review payloads before submit");
assert.match(app, /\/api\/review\/latest/, "app should load latest human review status");
assert.match(app, /\/api\/review\/human/, "app should submit human review payloads through the API");
assert.match(app, /\/api\/review\/media/, "app should preview only safe review media through the API");
assert.match(app, /renderOcrQaReviewPanel/, "app should render the OCR QA review panel");
assert.match(app, /validateOcrQaReviewForm/, "app should validate OCR QA payloads before submit");
assert.match(app, /\/api\/ocr-qa\/latest/, "app should load latest OCR QA manifest status");
assert.match(app, /\/api\/ocr-qa\/review/, "app should submit OCR QA review payloads through the API");
assert.match(app, /\/api\/ocr-qa\/crop/, "app should preview only safe OCR QA crop thumbnails through the API");
assert.match(hardening, /HUMAN_REVIEW_CRITERIA/, "hardening should define the shared human review criteria contract");
assert.match(hardening, /HUMAN_REVIEW_FLAGS/, "hardening should define the shared human review flag contract");
assert.match(hardening, /validateHumanReviewInput/, "hardening should validate human review payloads");
assert.match(hardening, /validateOcrQaReviewInput/, "hardening should validate OCR QA review payloads");
assert.match(hardening, /OCR_QA_REVIEW_LEAK_GUARD/, "hardening should reject unsafe OCR QA review content");
assert.match(hardening, /textBlocksAction/, "human review flags should include text-blocks-action");
assert.match(hardening, /missingPayoff/, "human review flags should include missing-payoff");
assert.match(hardening, /reactionOnly/, "human review flags should include reaction-only");
assert.match(css, /\.human-review-form/, "CSS should style the human review form");
assert.match(css, /\.human-review-videos/, "CSS should style human review video previews");
assert.match(css, /\.human-review-score/, "CSS should style human review score controls");
assert.match(css, /\.ocr-qa-panel/, "CSS should style the OCR QA panel");
assert.match(css, /\.ocr-qa-crops/, "CSS should style OCR QA crop thumbnails");
assert.match(css, /\.ocr-qa-calibration/, "CSS should style OCR QA calibration metrics");
assert.match(gitignore, /^manual-downloads\/$/m, "manual generated and reference videos should stay out of git");
assert.match(browserSmoke, /browser-latest\.json|BROWSER_LATEST/, "browser smoke should write a browser latest report");
assert.match(browserSmoke, /REQUIRED_TEST_IDS/, "browser smoke should assert stable selectors");
assert.match(browserSmoke, /manualChecklistRequired/, "browser smoke should document dependency-light fallback");
assert.match(browserSmoke, /api_demo_smoke_passed/, "browser smoke should include the API E2E fallback");
assert.match(browserSmoke, /findSensitiveLeak/, "browser smoke should fail closed with safe leak metadata");
assert.match(packageJson, /"demo:browser:e2e": "node demo\/run-playwright-smoke\.mjs"/, "package should expose the Playwright browser E2E script");
assert.match(packageJson, /"branch:doctor": "node tools\/release\/check-branch-protection\.mjs"/, "package should expose branch policy doctor");
assert.match(packageJson, /"branch:proof": "node tools\/release\/write-branch-protection-proof\.mjs"/, "package should expose branch policy proof generation");
assert.match(packageJson, /"branch:setup": "node tools\/release\/print-branch-ruleset-setup\.mjs"/, "package should expose branch ruleset setup guidance");
assert.match(packageJson, /"demo:browser:install": "playwright install chromium"/, "package should expose a Playwright Chromium install helper");
assert.match(packageJson, /"ci:reports": "node demo\/validate-ci-reports\.mjs"/, "package should expose CI report validation");
assert.match(packageJson, /"env:check": "node tools\/release\/check-environment\.mjs"/, "package should expose staging environment validation");
assert.match(packageJson, /"feedback:summary": "node eval\/run-feedback-summary\.mjs"/, "package should expose the human feedback summary runner");
assert.match(packageJson, /"github:doctor": "node tools\/release\/check-github-cli\.mjs"/, "package should expose GitHub CLI doctor");
assert.match(packageJson, /"github:setup": "node tools\/release\/print-github-cli-setup\.mjs"/, "package should expose GitHub CLI setup guidance");
assert.match(packageJson, /"ocr:doctor": "node tools\/release\/check-ocr-runtime\.mjs"/, "package should expose OCR runtime doctor");
assert.match(packageJson, /"ocr:smoke": "node demo\/run-ocr-smoke\.mjs"/, "package should expose OCR smoke proof");
assert.match(packageJson, /"render:check": "node tools\/release\/check-render-staging\.mjs"/, "package should expose Render staging configuration validation");
assert.match(packageJson, /"render:manual": "node tools\/release\/print-render-staging-checklist\.mjs"/, "package should expose the live Render manual checklist");
assert.match(packageJson, /"render:proof": "node tools\/release\/render-staging-proof\.mjs"/, "package should expose the no-network Render staging proof");
assert.match(packageJson, /"staging:check": "node tools\/release\/check-staging-readiness\.mjs"/, "package should expose staging readiness validation");
assert.match(packageJson, /"staging:deploy": "node tools\/release\/staging-deploy\.mjs"/, "package should expose provider-specific staging deploy");
assert.match(packageJson, /"staging:smoke": "node tools\/release\/check-staging-smoke\.mjs"/, "package should expose deployed staging smoke");
assert.match(packageJson, /"staging:smoke:full": "node tools\/release\/check-staging-full-smoke\.mjs"/, "package should expose opt-in full staging smoke");
assert.match(packageJson, /"staging:smoke:cleanup": "node tools\/release\/cleanup-staging-full-smoke\.mjs"/, "package should expose opt-in full staging smoke cleanup");
assert.match(packageJson, /"release:check": "node tools\/release\/verify-release-gate\.mjs"/, "package should expose release gate verification");
assert.match(packageJson, /"release:evidence": "node tools\/release\/write-release-evidence\.mjs"/, "package should expose release evidence generation");
assert.match(packageJson, /"release:readiness": "node tools\/release\/check-release-readiness\.mjs"/, "package should expose release readiness health validation");
assert.match(packageJson, /"remote:ci": "node tools\/release\/check-remote-ci\.mjs"/, "package should expose remote CI verification");
assert.match(packageJson, /"remote:ci:proof": "node tools\/release\/write-remote-ci-proof\.mjs"/, "package should expose remote CI proof generation");
assert.match(packageJson, /"youtube:doctor": "node tools\/release\/check-youtube-ingest\.mjs"/, "package should expose the YouTube ingest doctor");
assert.match(packageJson, /"youtube:smoke": "node demo\/run-youtube-smoke\.mjs"/, "package should expose the opt-in YouTube smoke");
assert.match(packageJson, /"youtube:e2e:local": "node demo\/run-youtube-live-e2e\.mjs"/, "package should expose the opt-in local YouTube E2E proof");
assert.match(packageJson, /"youtube:proof": "node demo\/run-youtube-live-e2e\.mjs"/, "package should expose the operator YouTube proof alias");
assert.match(packageJson, /"youtube:proof:operator": "node demo\/run-youtube-live-e2e\.mjs --operator"/, "package should expose the explicit operator YouTube proof command");
assert.match(packageJson, /"demo:youtube:live": "node demo\/run-youtube-live-e2e\.mjs"/, "package should expose the demo YouTube live proof alias");
assert.match(packageJson, /"demo:human-review": "node demo\/run-human-visual-review\.mjs"/, "package should expose the human visual review runner");
assert.match(packageJson, /"playwright"/, "Playwright should be a scoped dev dependency for browser E2E");
assert.match(playwrightSmoke, /setInputFiles/, "Playwright smoke should upload the fixture through the browser context");
assert.match(playwrightSmoke, /youtube-ingest-button/, "Playwright smoke should cover the disabled YouTube ingest path");
assert.match(app, /createYouTubeWarningMessage/, "app should render safe YouTube ingest warnings");
assert.match(app, /createYouTubeRecoveryMessage/, "app should render safe YouTube recovery guidance");
assert.match(app, /retryYoutubeIngestBtn/, "app should wire YouTube retry recovery");
assert.match(app, /uploadMp4FallbackBtn/, "app should wire YouTube MP4 fallback recovery");
assert.match(hardening, /YOUTUBE_BOT_CHECK_REQUIRED/, "frontend hardening should know bot-check YouTube failures");
assert.match(hardening, /createYouTubeRecoveryMessage/, "frontend hardening should expose YouTube recovery copy");
assert.match(youtubeDownloaderErrors, /classifyYouTubeDownloaderFailure/, "YouTube downloader failures should be classified at the adapter boundary");
assert.match(youtubeDownloaderErrors, /YOUTUBE_AUTH_REQUIRED/, "YouTube classifier should distinguish auth-required failures");
assert.match(youtubeDownloaderErrors, /YOUTUBE_BOT_CHECK_REQUIRED/, "YouTube classifier should distinguish bot-check failures");
assert.match(youtubeDownloaderErrors, /YOUTUBE_COOKIES_REQUIRED/, "YouTube classifier should distinguish cookie-required failures");
assert.match(youtubeDownloaderErrors, /YOUTUBE_VIDEO_PRIVATE/, "YouTube classifier should distinguish private videos");
assert.match(youtubeDownloaderErrors, /YOUTUBE_RATE_LIMITED/, "YouTube classifier should distinguish rate limits");
assert.doesNotMatch(youtubeDownloaderErrors, /throw new Error\(.*stderr|rawError|rawOutput/, "YouTube classifier should not throw raw downloader output");
assert.match(localYoutubeAdapter, /metadataWarningFromFailure/, "local YouTube metadata should surface safe ingest-risk warnings");
assert.match(playwrightSmoke, /SHORTSENGINE_YOUTUBE_LIVE_E2E_BROWSER/, "Playwright smoke should expose an opt-in YouTube live browser path");
assert.match(playwrightSmoke, /checkEnvironment/, "Playwright live YouTube path should reuse the central env readiness gates");
assert.match(playwrightSmoke, /youtube_live_ingest_created_project_state/, "Playwright live path should verify ingest-created project state");
assert.match(playwrightSmoke, /getByTestId\("download-link"\)/, "Playwright smoke should assert download UI state by stable selector");
assert.match(playwrightSmoke, /download_endpoint_returns_video/, "Playwright smoke should verify the rendered MP4 endpoint");
assert.match(playwrightSmoke, /findSensitiveLeak/, "Playwright smoke should fail closed before writing unsafe reports");
assert.match(playwrightSmoke, /PLAYWRIGHT_NOT_AVAILABLE/, "Playwright smoke should report missing runtime safely");
assert.match(playwrightSmoke, /captureFailureScreenshot/, "Playwright smoke should capture screenshots only for failed E2E runs");
assert.match(playwrightSmoke, /SHORTSENGINE_BROWSER_E2E_TRACE/, "Playwright trace capture should require an explicit env flag");
assert.match(playwrightSmoke, /SHORTSENGINE_BROWSER_E2E_VIDEO/, "Playwright video capture should require an explicit env flag");
assert.match(playwrightSmoke, /cleanupPlaywrightArtifacts/, "Playwright smoke should keep report and artifact retention bounded");
for (const [label, script] of Object.entries({ demoSmoke, playwrightSmoke, youtubeLiveE2E })) {
  assert.doesNotMatch(
    script,
    /MATCHCUTS_PERSISTENCE_ADAPTER\s*:\s*"sqlite"/,
    `${label} should not force sqlite because Node 20 CI may not provide node:sqlite`,
  );
}
assert.match(reportSafety, /SIGNED_DOWNLOAD_TOKEN_RE/, "report safety should treat signed download tokens as sensitive in persisted reports");
assert.match(reportSafety, /URL_SECRET_QUERY/, "report safety should treat URL query credentials as sensitive in persisted reports");
assert.match(reportSafety, /SECRET_QUERY_KEYS/, "report safety should keep query credential names centralized");
assert.match(reportSafety, /APP_SECRET_VALUE/, "report safety should catch app/provider secret values without relying only on unsafe keys");
assert.match(reportSafety, /RENDER_SERVICE_ID/, "report safety should treat Render service ids as sensitive in persisted reports");
assert.match(reportSafety, /GITHUB_TOKEN/, "report safety should treat GitHub tokens as sensitive in persisted reports");
assert.match(reportSafety, /GITLAB_TOKEN/, "report safety should treat GitLab tokens as sensitive in persisted reports");
assert.match(reportSafety, /SLACK_TOKEN/, "report safety should treat Slack tokens as sensitive in persisted reports");
assert.match(reportSafety, /PRIVATE_KEY_BLOCK/, "report safety should reject private key blocks in persisted reports");
assert.match(reportSafety, /YOUTUBE_COOKIE_VALUE/, "report safety should reject raw YouTube cookie values in persisted reports");
assert.match(reportSafety, /serviceid/, "report safety should reject raw service id keys");
assert.match(reportSafety, /rawlogs/, "report safety should reject raw log keys");
assert.match(reportSafety, /privatekey/, "report safety should reject private key fields");
assert.match(reportSafety, /cookies/, "report safety should reject cookie fields");
assert.match(reportSafety, /UNSAFE_KEYS/, "report safety should block unsafe internal report keys");
assert.match(reportSafety, /redactForLogs/, "report safety should reuse the server log redaction behavior");
assert.match(ciReportValidator, /findSensitiveLeak/, "CI report validator should reuse leak detection");
assert.match(ciReportValidator, /CI_REPORT_STALE/, "CI report validator should reject stale reports");
assert.match(ciReportValidator, /Passing Playwright runs must not publish artifact files/, "CI report validator should enforce failure-only Playwright artifacts");
assert.match(ciReportValidator, /Passing Playwright runs must not leave failure artifact files/, "CI report validator should reject stale Playwright failure artifacts");
assert.match(ciReportValidator, /demo\/results\/playwright-artifacts/, "CI report validator should require managed Playwright artifact refs");
assert.match(envChecker, /ENV_CONTRACT/, "environment checker should define an explicit env contract");
assert.match(envChecker, /ENV_PROVIDER_CREDENTIAL_MISSING/, "environment checker should reject real providers without credentials");
assert.match(envChecker, /ENV_CLOUD_STORAGE_INCOMPLETE/, "environment checker should reject incomplete cloud storage config");
assert.match(envChecker, /ENV_BROWSER_SKIP_UNSAFE/, "environment checker should reject browser skip flags");
assert.match(envChecker, /ENV_YOUTUBE_LIVE_E2E_INGEST_DISABLED/, "environment checker should require ingest before live YouTube proof");
assert.match(envChecker, /ENV_YOUTUBE_LIVE_E2E_RIGHTS_REQUIRED/, "environment checker should require rights before live YouTube proof");
assert.match(envChecker, /ENV_YOUTUBE_LIVE_E2E_URL_NOT_ALLOWED/, "environment checker should require allowlist or manual gate for live YouTube proof");
assert.match(envChecker, /normalizeYouTubeUrl/, "environment checker should validate live YouTube proof URLs through the domain parser");
assert.match(envChecker, /findSensitiveLeak/, "environment checker should guard readiness output against leaks");
assert.match(stagingReadiness, /STAGING_ENV_CONTRACT/, "staging readiness should define an explicit staging env contract");
assert.match(stagingReadiness, /STAGING_URL_CREDENTIALS_UNSAFE/, "staging readiness should reject credentialed URLs");
assert.match(stagingReadiness, /STAGING_URL_LOCAL_UNSAFE/, "staging readiness should reject localhost without explicit local mode");
assert.match(stagingReadiness, /STAGING_SERVICE_ID_MISSING/, "staging readiness should require Render service id for Render deploys");
assert.match(stagingReadiness, /hostNetworkType/, "staging readiness should classify local private and remote hosts");
assert.match(stagingReadiness, /first === 169 && second === 254/, "staging readiness should reject link-local metadata-style targets without explicit local mode");
assert.match(stagingReadiness, /verifyStagingWorkflowContract/, "staging readiness should validate the staging workflow contract");
assert.match(stagingReadiness, /findSensitiveLeak/, "staging readiness should guard summaries against leaks");
assert.match(stagingSmoke, /SHORTSENGINE_STAGING_URL/, "staging smoke should require an explicit deployed URL");
assert.match(stagingSmoke, /GET/, "staging smoke should perform a health GET");
assert.match(stagingSmoke, /MAX_HEALTH_RESPONSE_BYTES/, "staging smoke should bound health response size");
assert.match(stagingSmoke, /STAGING_HEALTH_JSON_INVALID/, "staging smoke should reject invalid JSON safely");
assert.match(stagingSmoke, /validateHealthPayload/, "staging smoke should validate health response shape");
assert.match(stagingSmoke, /findSensitiveLeak/, "staging smoke should guard health and summary output against leaks");
assert.match(renderCheck, /RENDER_RUNTIME_CONTRACT/, "Render check should define an explicit runtime contract");
assert.match(renderCheck, /buildCommand: "npm ci"/, "Render check should document the build command");
assert.match(renderCheck, /startCommand: "npm start"/, "Render check should document the start command");
assert.match(renderCheck, /healthCheckPath: "\/health"/, "Render check should document the health check path");
assert.match(renderCheck, /networkCalls: false/, "Render check should stay no-network");
assert.match(renderCheck, /RENDER_STAGING_URL_PUBLIC_REQUIRED/, "Render check should require a public staging URL");
assert.match(renderCheck, /findSensitiveLeak/, "Render check should guard summaries against leaks");
assert.match(renderManual, /RENDER_STAGING_CHECKLIST/, "Render manual helper should define a stable checklist");
assert.match(renderManual, /autoDeployRecommendation/, "Render manual helper should document auto deploy guidance");
assert.match(renderManual, /copy-from-render-dashboard/, "Render manual helper should avoid real-looking service ids");
assert.match(renderManual, /secretsIncluded: false/, "Render manual helper should declare that it includes no secrets");
assert.match(renderManual, /findSensitiveLeak/, "Render manual helper should guard checklist output against leaks");
assert.match(renderProof, /proofEnvironment/, "Render proof should force a local provider-none environment");
assert.match(renderProof, /SHORTSENGINE_STAGING_DEPLOY_PROVIDER: "none"/, "Render proof should force provider none");
assert.match(renderProof, /networkCalls: false/, "Render proof should stay no-network");
assert.match(renderProof, /runStagingDeploy/, "Render proof should cover staging deploy in readiness-only mode");
assert.match(renderProof, /findSensitiveLeak/, "Render proof should guard proof output against leaks");
assert.match(stagingDeploy, /SUPPORTED_DEPLOY_PROVIDERS/, "staging deploy should define explicit supported providers");
assert.match(stagingDeploy, /render/, "staging deploy should support Render as the first provider");
assert.match(stagingDeploy, /api\.render\.com\/v1/, "staging deploy should target the Render deploy API");
assert.match(stagingDeploy, /STAGING_DEPLOY_PROVIDER_UNSUPPORTED/, "staging deploy should fail closed for unsupported providers");
assert.match(stagingDeploy, /validateRenderServiceId/, "staging deploy should validate the Render service id");
assert.match(stagingDeploy, /MAX_RENDER_DEPLOY_RESPONSE_BYTES/, "staging deploy should bound Render provider response bodies");
assert.match(stagingDeploy, /STAGING_RENDER_DEPLOY_RESPONSE_TOO_LARGE/, "staging deploy should reject oversized Render responses safely");
assert.match(stagingDeploy, /STAGING_RENDER_DEPLOY_JSON_INVALID/, "staging deploy should reject invalid Render JSON safely");
assert.match(stagingDeploy, /safeDeployStatus/, "staging deploy should sanitize provider status strings");
assert.match(stagingDeploy, /findSensitiveLeak/, "staging deploy should guard summaries against leaks");
assert.match(githubDoctor, /gh/, "GitHub doctor should use GitHub CLI");
assert.match(githubDoctor, /execFile/, "GitHub doctor should avoid shell-based command execution");
assert.match(githubDoctor, /GITHUB_CLI_MISSING/, "GitHub doctor should report missing gh safely");
assert.match(githubDoctor, /GITHUB_AUTH_MISSING/, "GitHub doctor should report missing auth safely");
assert.match(githubDoctor, /GITHUB_NETWORK_UNAVAILABLE/, "GitHub doctor should classify network failures safely");
assert.match(githubDoctor, /GITHUB_REPO_UNREADABLE/, "GitHub doctor should report unreadable repos safely");
assert.match(githubDoctor, /GITHUB_ACTIONS_UNREADABLE/, "GitHub doctor should report unreadable Actions safely");
assert.match(githubDoctor, /GITHUB_OUTPUT_UNSAFE/, "GitHub doctor should fail closed on unsafe output");
assert.match(githubDoctor, /phaseForCode/, "GitHub doctor should classify safe failure phases");
assert.match(githubDoctor, /passed: false/, "GitHub doctor failures should include a passed boolean");
assert.match(githubDoctor, /skipped: false/, "GitHub doctor failures should include a skipped boolean");
assert.match(githubDoctor, /branchProtectionReadiness/, "GitHub doctor should check branch protection readiness read-only");
assert.match(githubDoctor, /remoteMutation: false/, "GitHub doctor should declare no remote mutation");
assert.match(githubDoctor, /logsDownloaded: false/, "GitHub doctor should not download logs");
assert.match(githubDoctor, /artifactsDownloaded: false/, "GitHub doctor should not download artifacts");
assert.match(githubDoctor, /run-npm-run-github-setup/, "GitHub doctor missing-gh failures should point at setup guidance");
assert.match(githubDoctor, /operatorRecoveryForCode/, "GitHub doctor should attach safe operator recovery commands");
assert.match(githubDoctor, /brew install gh/, "GitHub doctor missing-gh recovery should include the macOS install command");
assert.match(githubDoctor, /gh auth login/, "GitHub doctor missing-auth recovery should include manual auth guidance");
assert.doesNotMatch(githubDoctor, /ghp_|github_pat_|GITHUB_TOKEN\s*=|secret set|repo edit|--method\s+(?:PATCH|PUT|DELETE)|download-logs|view --log|artifact download/i, "GitHub doctor must not hardcode tokens, mutate remotes or download raw logs/artifacts");
assert.match(githubSetup, /buildGithubCliSetupGuide/, "GitHub setup should expose a testable guide builder");
assert.match(githubSetup, /documentation-only/, "GitHub setup should be documentation-only");
assert.match(githubSetup, /networkCalls: false/, "GitHub setup should declare no network calls");
assert.match(githubSetup, /authStarted: false/, "GitHub setup should not start auth automatically");
assert.match(githubSetup, /remoteMutation: false/, "GitHub setup should not mutate remotes");
assert.match(githubSetup, /tokensRequested: false/, "GitHub setup should not request tokens");
assert.match(githubSetup, /logsDownloaded: false/, "GitHub setup should not download logs");
assert.match(githubSetup, /artifactsDownloaded: false/, "GitHub setup should not download artifacts");
assert.match(githubSetup, /gh auth login/, "GitHub setup should explain manual auth");
assert.match(githubSetup, /gh auth status/, "GitHub setup should include auth status verification");
assert.match(githubSetup, /anaschatz\/Shorts-Engine/, "GitHub setup should document the expected repository");
assert.match(githubSetup, /https:\/\/cli\.github\.com\//, "GitHub setup should include the official GitHub CLI install page");
assert.match(githubSetup, /repo metadata read/, "GitHub setup should document read-only repository metadata access");
assert.match(githubSetup, /actions read/, "GitHub setup should document Actions read access");
assert.match(githubSetup, /npm run github:doctor/, "GitHub setup should include the doctor command");
assert.match(githubSetup, /npm run remote:ci/, "GitHub setup should include remote CI verification");
assert.match(githubSetup, /npm run remote:ci:proof/, "GitHub setup should include remote CI proof");
assert.match(githubSetup, /findSensitiveLeak/, "GitHub setup should guard output against leaks");
assert.doesNotMatch(githubSetup, /execFile|spawn|child_process|ghp_|github_pat_|GITHUB_TOKEN\s*=|secret set|repo edit|--method\s+(?:PATCH|PUT|DELETE)|download-logs|view --log|artifact download/i, "GitHub setup must not run commands, hardcode tokens, mutate remotes or download raw logs/artifacts");
assert.match(remoteCi, /gh/, "remote CI verifier should use GitHub CLI");
assert.match(remoteCi, /execFile/, "remote CI verifier should avoid shell-based command execution");
assert.match(remoteCi, /SHORTSENGINE_REMOTE_CI_TIMEOUT_MS/, "remote CI verifier should bound polling timeout");
assert.match(remoteCi, /SHORTSENGINE_REMOTE_CI_POLL_INTERVAL_MS/, "remote CI verifier should bound polling interval");
assert.match(remoteCi, /runRemoteCiCheck/, "remote CI verifier should expose a testable runner");
assert.match(remoteCi, /GITHUB_CLI_MISSING/, "remote CI verifier should use the shared missing-gh code");
assert.match(remoteCi, /GITHUB_AUTH_MISSING/, "remote CI verifier should use the shared missing-auth code");
assert.match(remoteCi, /assertRunMatchesContext/, "remote CI verifier should assert the run belongs to the current commit");
assert.match(remoteCi, /REMOTE_CI_SHA_MISMATCH/, "remote CI verifier should fail closed on commit SHA mismatch");
assert.match(remoteCi, /REMOTE_CI_NETWORK_UNAVAILABLE/, "remote CI verifier should classify network failures safely");
assert.match(remoteCi, /statusForDetails/, "remote CI verifier should classify success pending failure and cancelled states");
assert.match(remoteCi, /phaseForFailureCode/, "remote CI verifier should classify failure phases");
assert.match(remoteCi, /headSha/, "remote CI verifier should read and report the workflow run head SHA");
assert.match(remoteCi, /waitedMs/, "remote CI verifier should report bounded wait metadata");
assert.match(remoteCi, /findSensitiveLeak/, "remote CI verifier should guard summaries against leaks");
assert.match(remoteCi, /logsDownloaded: false/, "remote CI verifier should not download logs by default");
assert.match(remoteCi, /artifactsDownloaded: false/, "remote CI verifier should not download artifacts by default");
assert.match(remoteCi, /operatorRecoveryForCode/, "remote CI verifier should attach safe operator recovery commands");
assert.match(remoteCi, /brew install gh/, "remote CI missing-gh recovery should include the macOS install command");
assert.match(remoteCi, /gh auth login/, "remote CI missing-auth recovery should include manual auth guidance");
assert.doesNotMatch(remoteCi, /ghp_|github_pat_|GITHUB_TOKEN\s*=|staging:deploy|staging:smoke:full|download-logs|view --log|artifact download/i, "remote CI verifier must not hardcode tokens, deploy, full smoke or download raw logs/artifacts");
assert.match(remoteCiProof, /runRemoteCiCheck/, "remote CI proof should build on the verifier");
assert.match(remoteCiProof, /buildRemoteCiFailureProof/, "remote CI proof should write safe failure proofs for missing gh/auth/no-run cases");
assert.match(remoteCiProof, /validateRemoteCiSummaryForProof/, "remote CI proof should validate verifier summary shape before writing reports");
assert.match(remoteCiProof, /REMOTE_CI_PROOF_SUMMARY_INVALID/, "remote CI proof should fail closed on malformed verifier summaries");
assert.match(remoteCiProof, /remote-ci-latest\.json/, "remote CI proof should write a stable latest proof report");
assert.match(remoteCiProof, /command:\s*DEFAULT_COMMAND_NAME/, "remote CI proof should include the proof command");
assert.match(remoteCiProof, /triage:\s*buildRemoteCiTriage/, "remote CI proof should include report-driven triage");
assert.match(remoteCiProof, /passed:\s*safeSummary\.ok/, "remote CI proof should include a passed boolean");
assert.match(remoteCiProof, /skipped:\s*false/, "remote CI proof should declare that remote proof is not skipped");
assert.match(remoteCiProof, /failure/, "remote CI proof should include a structured safe failure section");
assert.match(remoteCiProof, /safeOperatorRecovery/, "remote CI proof should validate operator recovery commands before persistence");
assert.match(remoteCiProof, /rawLogsRequired: false/, "remote CI proof should not require raw logs by default");
assert.match(remoteCiProof, /rawArtifactsRequired: false/, "remote CI proof should not require raw artifacts by default");
assert.match(remoteCiProof, /findSensitiveLeak/, "remote CI proof should guard proof output against leaks");
assert.match(remoteCiProof, /logsDownloaded: false/, "remote CI proof should not download logs");
assert.match(remoteCiProof, /artifactsDownloaded: false/, "remote CI proof should not download artifacts");
assert.doesNotMatch(remoteCiProof, /ghp_|github_pat_|GITHUB_TOKEN\s*=|download-logs|view --log|artifact download|repo edit|secret set|--method\s+(?:PATCH|PUT|DELETE)/i, "remote CI proof must not hardcode tokens, mutate remotes or download raw logs/artifacts");
assert.match(branchPolicy, /runGithubCliDoctor/, "branch policy doctor should reuse GitHub CLI doctor readiness");
assert.match(branchPolicy, /rulesetReadiness/, "branch policy doctor should evaluate repository rulesets");
assert.match(branchPolicy, /GITHUB_RULESET_UNREADABLE/, "branch policy doctor should classify unreadable rulesets safely");
assert.match(branchPolicy, /BRANCH_PROTECTION_LATEST_RELATIVE_PATH/, "branch policy doctor should define the stable proof path");
assert.match(branchPolicy, /EXPECTED_BRANCH_POLICY/, "branch policy doctor should centralize the expected release policy");
assert.match(branchPolicy, /git"\s*,\s*\["ls-remote"/, "branch policy doctor should verify the remote branch SHA read-only");
assert.match(branchPolicy, /logsDownloaded: false/, "branch policy doctor should not download logs");
assert.match(branchPolicy, /artifactsDownloaded: false/, "branch policy doctor should not download artifacts");
assert.match(branchPolicy, /remoteMutation: false/, "branch policy doctor should declare no remote mutation");
assert.match(branchPolicy, /findSensitiveLeak/, "branch policy doctor should guard summaries against leaks");
assert.doesNotMatch(branchPolicy, /ghp_|github_pat_|GITHUB_TOKEN\s*=|secret set|repo edit|--method\s+(?:PATCH|PUT|DELETE)|download-logs|view --log|artifact download/i, "branch policy doctor must not hardcode tokens, mutate remotes or download raw logs/artifacts");
assert.match(branchPolicyProof, /BRANCH_PROTECTION_LATEST_RELATIVE_PATH/, "branch policy proof should write a stable latest report");
assert.match(branchPolicyProof, /runBranchProtectionCheck/, "branch policy proof should build on the read-only checker");
assert.match(branchPolicyProof, /command:\s*options\.commandName \|\| DEFAULT_COMMAND_NAME/, "branch policy proof should include the proof command");
assert.match(branchPolicyProof, /triage:\s*buildBranchPolicyTriage/, "branch policy proof should include report-driven triage");
assert.match(branchPolicyProof, /uiSetupReference/, "branch policy proof should include the operator UI setup reference");
assert.match(branchPolicyProof, /logsDownloaded: false/, "branch policy proof should not download logs");
assert.match(branchPolicyProof, /artifactsDownloaded: false/, "branch policy proof should not download artifacts");
assert.match(branchPolicyProof, /remoteMutation: false/, "branch policy proof should not mutate GitHub");
assert.match(branchPolicyProof, /findSensitiveLeak/, "branch policy proof should guard proof output against leaks");
assert.doesNotMatch(branchPolicyProof, /ghp_|github_pat_|GITHUB_TOKEN\s*=|secret set|repo edit|--method\s+(?:PATCH|PUT|DELETE)|download-logs|view --log|artifact download/i, "branch policy proof must not hardcode tokens, mutate remotes or download raw logs/artifacts");
assert.match(branchSetup, /documentation-only/, "branch ruleset setup should be documentation-only");
assert.match(branchSetup, /Settings -> Rules -> Rulesets/, "branch ruleset setup should include the GitHub UI navigation");
assert.match(branchSetup, /Create a new branch ruleset/, "branch ruleset setup should include the new ruleset step");
assert.match(branchSetup, /Release gate/, "branch ruleset setup should include the required status check");
assert.match(branchSetup, /Require pull request before merging/, "branch ruleset setup should include PR requirement guidance");
assert.match(branchSetup, /Require branches to be up to date before merging/, "branch ruleset setup should include up-to-date branch guidance");
assert.match(branchSetup, /Block force pushes/, "branch ruleset setup should include force-push protection guidance");
assert.match(branchSetup, /Block deletions/, "branch ruleset setup should include branch deletion protection guidance");
assert.match(branchSetup, /Require conversation resolution before merge/, "branch ruleset setup should include conversation resolution guidance");
assert.match(branchSetup, /networkCalls: false/, "branch ruleset setup should declare no network calls");
assert.match(branchSetup, /tokensRequested: false/, "branch ruleset setup should not request tokens");
assert.match(branchSetup, /rulesetMutation: false/, "branch ruleset setup should not mutate rulesets");
assert.match(branchSetup, /buildBranchRulesetSetupGuide/, "branch ruleset setup should expose a testable guide builder");
assert.match(branchSetup, /findSensitiveLeak/, "branch ruleset setup should guard output against leaks");
assert.doesNotMatch(branchSetup, /execFile|spawn|child_process|ghp_|github_pat_|GITHUB_TOKEN\s*=|secret set|repo edit|--method\s+(?:PATCH|PUT|DELETE)|download-logs|view --log|artifact download/i, "branch ruleset setup must not run commands, hardcode tokens, mutate remotes or download raw logs/artifacts");
assert.match(releaseReadiness, /REQUIRED_RELEASE_SCRIPTS/, "release readiness should define required release scripts");
assert.match(releaseReadiness, /local-static-readiness/, "release readiness should stay local and static");
assert.match(releaseReadiness, /networkCalls: false/, "release readiness should declare no network calls");
assert.match(releaseReadiness, /authStarted: false/, "release readiness should not start auth");
assert.match(releaseReadiness, /remoteMutation: false/, "release readiness should not mutate GitHub");
assert.match(releaseReadiness, /tokensRequested: false/, "release readiness should not request tokens");
assert.match(releaseReadiness, /logsDownloaded: false/, "release readiness should not download logs");
assert.match(releaseReadiness, /artifactsDownloaded: false/, "release readiness should not download artifacts");
assert.match(releaseReadiness, /safeMissingDependencyCode: "GITHUB_CLI_MISSING"/, "release readiness should document safe missing gh behavior");
assert.match(releaseReadiness, /safeMissingAuthCode: "GITHUB_AUTH_MISSING"/, "release readiness should document safe missing auth behavior");
assert.match(releaseReadiness, /exactCommitRequired: true/, "release readiness should document exact commit proof requirement");
assert.match(releaseReadiness, /failureProofReports: true/, "release readiness should document safe failure proof reports");
assert.match(releaseReadiness, /branchPolicyProof/, "release readiness should document branch policy proof capability");
assert.match(releaseReadiness, /setupCommand: "npm run branch:setup"/, "release readiness should document branch setup guidance");
assert.match(releaseReadiness, /branch-protection-latest\.json/, "release readiness should expose the branch policy proof report path");
assert.match(releaseReadiness, /safeUnknownRulesetCode: "GITHUB_RULESET_UNREADABLE"/, "release readiness should document safe unknown ruleset behavior");
assert.doesNotMatch(releaseReadiness, /execFile|spawn|child_process|GITHUB_TOKEN\s*=|secret set|repo edit|--method\s+(?:PATCH|PUT|DELETE)|download-logs|view --log|artifact download|ghp_[A-Za-z0-9_]{20,}|github_pat_[A-Za-z0-9_]{20,}/i, "release readiness must not run commands, hardcode tokens, mutate remotes or download raw logs/artifacts");
assert.match(releaseReadinessCheck, /createReleaseReadiness/, "release readiness check should reuse the shared readiness module");
assert.match(releaseReadinessCheck, /result\.ready !== true/, "release readiness check should fail closed when the contract is invalid");
assert.match(stagingFullSmoke, /SHORTSENGINE_STAGING_FULL_SMOKE/, "full staging smoke should require an explicit opt-in flag");
assert.match(stagingFullSmoke, /validateStagingUrl/, "full staging smoke should reuse staging URL validation");
assert.match(stagingFullSmoke, /validateFixturePath/, "full staging smoke should validate fixture safety");
assert.match(stagingFullSmoke, /createMultipartBody/, "full staging smoke should upload the fixture through multipart form data");
assert.match(stagingFullSmoke, /rightsConfirmed: true/, "full staging smoke should confirm fixture rights explicitly");
assert.match(stagingFullSmoke, /pollJob/, "full staging smoke should poll the generated job");
assert.match(stagingFullSmoke, /validateMp4/, "full staging smoke should validate downloaded MP4 signatures");
assert.match(stagingFullSmoke, /durabilityMode/, "full staging smoke should report durable-capable versus ephemeral staging mode");
assert.match(stagingFullSmoke, /STAGING_FULL_SMOKE_SOURCE/, "full staging smoke should mark created resources with a cleanup source");
assert.match(stagingFullSmoke, /STAGING_FULL_SMOKE_IDEMPOTENCY_PREFIX/, "full staging smoke should use a cleanup-identifiable idempotency prefix");
assert.match(stagingFullSmoke, /findSensitiveLeak/, "full staging smoke should guard summaries and responses against leaks");
assert.match(stagingFullCleanup, /SHORTSENGINE_STAGING_FULL_SMOKE_CLEANUP/, "full smoke cleanup should require an explicit delete flag");
assert.match(stagingFullCleanup, /dryRun: !deleteEnabled/, "full smoke cleanup should be dry-run by default");
assert.match(stagingFullCleanup, /STAGING_FULL_SMOKE_SOURCE/, "full smoke cleanup should match the full-smoke source marker");
assert.match(stagingFullCleanup, /deleteMarkedArtifact/, "full smoke cleanup should delete only through the marked artifact boundary");
assert.match(stagingFullCleanup, /ACTIVE_JOB_STATUSES/, "full smoke cleanup should protect queued and processing jobs");
assert.match(stagingFullCleanup, /findSensitiveLeak/, "full smoke cleanup should guard summaries against leaks");
assert.equal(
  [renderCheck, renderManual, renderProof].filter((text) => /api\.render\.com\/v1/.test(text)).length,
  0,
  "Render check/manual/proof helpers must not call Render APIs",
);
assert.match(envDocs, /Staging Readiness Checklist/, "environment docs should include a staging readiness checklist");
assert.match(envDocs, /npm run env:check/, "environment docs should document the env check command");
assert.match(envDocs, /npm run staging:check/, "environment docs should document the staging readiness command");
assert.match(envDocs, /npm run render:check/, "environment docs should document the Render readiness command");
assert.match(envDocs, /npm run render:proof/, "environment docs should document the Render no-network proof command");
assert.match(envDocs, /staging:smoke:full/, "environment docs should document the opt-in full staging smoke command");
assert.match(envDocs, /staging:smoke:cleanup/, "environment docs should document the opt-in full staging smoke cleanup command");
assert.match(envDocs, /SHORTSENGINE_STAGING_FULL_SMOKE/, "environment docs should document the full staging smoke opt-in flag");
assert.match(envDocs, /SHORTSENGINE_STAGING_FULL_SMOKE_CLEANUP/, "environment docs should document the full staging smoke cleanup flag");
assert.match(envDocs, /SHORTSENGINE_STAGING_URL/, "environment docs should document staging URL config");
assert.match(envDocs, /SHORTSENGINE_STAGING_DEPLOY_PROVIDER/, "environment docs should document staging provider config");
assert.match(envDocs, /SHORTSENGINE_STAGING_SERVICE_ID/, "environment docs should document Render service id config");
assert.match(envDocs, /MATCHCUTS_TRANSCRIPTION_PROVIDER/, "environment docs should document transcription provider config");
assert.match(envDocs, /MATCHCUTS_STORAGE_ADAPTER/, "environment docs should document storage adapter config");
assert.match(envDocs, /SHORTSENGINE_YOUTUBE_INGEST_ENABLED/, "environment docs should document the YouTube ingest opt-in flag");
assert.match(envDocs, /SHORTSENGINE_YOUTUBE_AUTHORIZED_IMPORT_ENABLED/, "environment docs should document the authorized import foundation flag");
assert.match(envDocs, /YOUTUBE_BOT_CHECK_REQUIRED/, "environment docs should document bot-check downloader failures");
assert.match(envDocs, /SHORTSENGINE_YOUTUBE_SMOKE=1/, "environment docs should document the explicit YouTube smoke flag");
assert.match(envDocs, /SHORTSENGINE_YOUTUBE_SMOKE_URL/, "environment docs should document the YouTube smoke URL");
assert.match(envDocs, /SHORTSENGINE_YOUTUBE_DOCTOR_URL/, "environment docs should document optional live health validation for YouTube doctor");
assert.match(envDocs, /disabled by default/i, "environment docs should document YouTube no-network default behavior");
assert.match(envDocs, /local staging/i, "environment docs should document YouTube local staging behavior");
assert.match(envDocs, /docs\/YOUTUBE_INGEST_MANUAL_SMOKE\.md/, "environment docs should link to manual YouTube ingest smoke guidance");
assert.match(envDocs, /OPENAI_API_KEY/, "environment docs should document provider credential handling");
assert.match(envExample, /MATCHCUTS_TRANSCRIPTION_PROVIDER=mock/, ".env.example should keep mock provider as the default");
assert.match(envExample, /MATCHCUTS_STORAGE_ADAPTER=local/, ".env.example should keep local storage as the default");
assert.match(envExample, /SHORTSENGINE_STAGING_DEPLOY_PROVIDER=none/, ".env.example should keep provider-neutral staging as the default");
assert.match(envExample, /SHORTSENGINE_STAGING_SERVICE_ID=/, ".env.example should leave Render service id empty by default");
assert.match(envExample, /SHORTSENGINE_STAGING_URL=/, ".env.example should leave staging URL empty by default");
assert.match(envExample, /SHORTSENGINE_STAGING_FULL_SMOKE=0/, ".env.example should keep full staging smoke disabled by default");
assert.match(envExample, /SHORTSENGINE_STAGING_FULL_SMOKE_CLEANUP=0/, ".env.example should keep full staging smoke cleanup deletion disabled by default");
assert.match(envExample, /SHORTSENGINE_OCR_QA_ARTIFACTS=0/, ".env.example should keep OCR QA artifacts disabled by default");
assert.match(envExample, /SHORTSENGINE_YOUTUBE_INGEST_ENABLED=0/, ".env.example should keep YouTube ingest disabled by default");
assert.match(envExample, /SHORTSENGINE_YOUTUBE_AUTHORIZED_IMPORT_ENABLED=0/, ".env.example should keep authorized YouTube import disabled by default");
assert.match(envExample, /SHORTSENGINE_YOUTUBE_SMOKE=0/, ".env.example should keep YouTube smoke disabled by default");
assert.match(envExample, /SHORTSENGINE_YOUTUBE_SMOKE_URL=/, ".env.example should leave the YouTube smoke URL empty by default");
assert.doesNotMatch(envExample, /sk-[A-Za-z0-9_-]{20,}|AKIA[A-Z0-9]{12,}|Bearer\s+[A-Za-z0-9._-]{10,}/, ".env.example must not contain real-looking secrets");
assert.match(releaseGateVerifier, /checkEnvironment/, "release gate verifier should include environment readiness");
assert.match(releaseGateVerifier, /github:doctor/, "release gate verifier should require the GitHub doctor script to exist");
assert.match(releaseGateVerifier, /github:setup/, "release gate verifier should require the GitHub setup script to exist");
assert.match(releaseGateVerifier, /branch:doctor/, "release gate verifier should require the branch policy doctor script to exist");
assert.match(releaseGateVerifier, /branch:proof/, "release gate verifier should require the branch policy proof script to exist");
assert.match(releaseGateVerifier, /branch:setup/, "release gate verifier should require the branch setup script to exist");
assert.match(releaseGateVerifier, /createReleaseReadiness/, "release gate verifier should include release readiness");
assert.match(releaseGateVerifier, /release:readiness/, "release gate verifier should require the release readiness script to exist");
assert.match(releaseGateVerifier, /render:check/, "release gate verifier should require the Render staging check script");
assert.match(releaseGateVerifier, /render:manual/, "release gate verifier should require the Render manual checklist script");
assert.match(releaseGateVerifier, /render:proof/, "release gate verifier should require the Render no-network proof script");
assert.match(releaseGateVerifier, /remote:ci/, "release gate verifier should require the remote CI script to exist");
assert.match(releaseGateVerifier, /remote:ci:proof/, "release gate verifier should require the remote CI proof script to exist");
assert.match(releaseGateVerifier, /ocr:doctor/, "release gate verifier should require OCR doctor");
assert.match(releaseGateVerifier, /ocr:smoke/, "release gate verifier should require OCR smoke proof");
assert.match(releaseGateVerifier, /ocr:qa:review/, "release gate verifier should require the OCR QA review proof");
assert.match(releaseGateVerifier, /staging:smoke:full/, "release gate verifier should require the full staging smoke script to exist");
assert.match(releaseGateVerifier, /staging:smoke:cleanup/, "release gate verifier should require the full smoke cleanup script to exist");
assert.match(releaseGateVerifier, /checkStagingReadiness/, "release gate verifier should include staging readiness");
assert.match(releaseGateVerifier, /validateCiReports/, "release gate verifier should reuse CI report validation");
assert.match(releaseGateVerifier, /REQUIRED_WORKFLOW_COMMANDS/, "release gate verifier should enforce required workflow commands");
assert.match(releaseGateVerifier, /SHORTSENGINE_BROWSER_E2E_ALLOW_SKIP/, "release gate verifier should reject browser skip flags");
assert.match(releaseGateVerifier, /integration:cloud\|MATCHCUTS_RUN_REAL_CLOUD_TESTS/, "release gate verifier should keep real cloud integration out of default CI");
assert.match(releaseGateVerifier, /FAILURE_ARTIFACT_ALLOWLIST/, "release gate verifier should enforce the artifact allowlist");
assert.match(releaseGateVerifier, /read-only-config-inspection/, "release gate verifier should inspect git remote state read-only");
assert.match(releaseGateVerifier, /BRANCH_PROTECTION_LATEST_RELATIVE_PATH/, "release gate verifier should include the branch policy proof path");
assert.match(releaseEvidenceWriter, /findSensitiveLeak/, "release evidence writer should reject sensitive evidence");
assert.match(releaseEvidenceWriter, /environmentReadiness/, "release evidence should include environment readiness");
assert.match(releaseEvidenceWriter, /stagingReadiness/, "release evidence should include staging readiness");
assert.match(releaseEvidenceWriter, /releaseReadiness/, "release evidence should include release readiness");
assert.match(releaseEvidenceWriter, /release\/results/, "release evidence writer should use the release results directory");
assert.doesNotMatch(releaseEvidenceWriter, /\bpath:\s*gate\.reports/, "release evidence should not expose report path keys directly");
assert.match(manualDocs, /npm run demo:fixture/, "manual docs should explain fixture generation");
assert.match(manualDocs, /npm run demo:browser/, "manual docs should explain browser smoke");
assert.match(manualDocs, /UPLOAD_EMPTY/, "manual docs should describe missing upload behavior");
assert.match(manualDocs, /YOUTUBE_RIGHTS_REQUIRED/, "manual docs should describe YouTube rights validation");
assert.match(manualDocs, /npm run youtube:doctor/, "manual docs should explain YouTube ingest doctor");
assert.match(manualDocs, /npm run youtube:smoke/, "manual docs should explain opt-in YouTube smoke");
assert.match(manualDocs, /npm run youtube:e2e:local/, "manual docs should explain opt-in local YouTube E2E proof");
assert.match(manualDocs, /YOUTUBE_DOWNLOADER_MISSING/, "manual docs should document YouTube downloader troubleshooting");
assert.match(manualDocs, /YOUTUBE_LIVE_E2E_SERVER_BIND_FAILED/, "manual docs should document local live server bind limitations");
assert.match(manualDocs, /docs\/YOUTUBE_INGEST_MANUAL_SMOKE\.md/, "manual docs should link to the detailed YouTube ingest smoke guide");
assert.match(manualDocs, /port already used/i, "manual docs should include troubleshooting");
assert.match(youtubeManualDocs, /Use only videos you own, have licensed, or are otherwise authorized/i, "YouTube manual smoke guide should document rights requirements");
assert.match(youtubeManualDocs, /never installs a downloader automatically/i, "YouTube manual smoke guide should keep downloader install manual");
assert.match(youtubeManualDocs, /npm run youtube:doctor/, "YouTube manual smoke guide should document the doctor command");
assert.match(youtubeManualDocs, /npm run youtube:smoke/, "YouTube manual smoke guide should document the smoke command");
assert.match(youtubeManualDocs, /npm run youtube:e2e:local/, "YouTube manual smoke guide should document the local live E2E command");
assert.match(youtubeManualDocs, /SHORTSENGINE_YOUTUBE_SMOKE=1/, "YouTube manual smoke guide should document explicit smoke opt-in");
assert.match(youtubeManualDocs, /SHORTSENGINE_YOUTUBE_LIVE_E2E=1/, "YouTube manual smoke guide should document explicit local live proof opt-in");
assert.match(youtubeManualDocs, /SHORTSENGINE_YOUTUBE_LIVE_E2E_RIGHTS_CONFIRMED=1/, "YouTube manual smoke guide should document local live rights confirmation");
assert.match(youtubeManualDocs, /demo\/results\/youtube-smoke-latest\.json/, "YouTube manual smoke guide should document report reading");
assert.match(youtubeManualDocs, /demo\/results\/youtube-live-e2e-latest\.json/, "YouTube manual smoke guide should document local live report reading");
assert.match(youtubeManualDocs, /generatedArtifact\.relativePath/, "YouTube manual smoke guide should document the generated artifact reference");
assert.match(youtubeManualDocs, /npm run demo:human-review/, "YouTube manual smoke guide should document human visual review after live proof");
assert.match(youtubeManualDocs, /YOUTUBE_SMOKE_REPORT_LEAK/, "YouTube manual smoke guide should document report leak failures");
assert.match(youtubeManualDocs, /YOUTUBE_SMOKE_HEALTH_NOT_READY/, "YouTube manual smoke guide should document health readiness failures");
assert.match(youtubeManualDocs, /YOUTUBE_DOWNLOADER_MISSING/, "YouTube manual smoke guide should document missing downloader failures");
assert.match(youtubeManualDocs, /YOUTUBE_BOT_CHECK_REQUIRED/, "YouTube manual smoke guide should document bot-check failures");
assert.match(youtubeManualDocs, /MP4 fallback/, "YouTube manual smoke guide should document the MP4 fallback recovery path");
assert.match(youtubeManualDocs, /Run Local UI E2E Demo/, "YouTube manual smoke guide should document the local UI E2E path");
assert.match(youtubeManualDocs, /Auto validation waits until URL and rights confirmation are present/, "YouTube manual smoke guide should document URL and rights gating");
assert.match(youtubeManualDocs, /Generate shorts` is disabled for YouTube sources until ingest creates project\/upload state/, "YouTube manual smoke guide should document generate gating");
assert.match(youtubeManualDocs, /Default CI and local checks must remain no-network and no-downloader/i, "YouTube manual smoke guide should keep CI default-safe");
assert.match(youtubeManualDocs, /SHORTSENGINE_YOUTUBE_LIVE_E2E_BROWSER=1/, "YouTube manual smoke guide should document the opt-in browser live path");
assert.match(youtubeManualDocs, /npm run env:check/, "YouTube manual smoke guide should run env validation before local live proof");
assert.match(envDocs, /SHORTSENGINE_YOUTUBE_LIVE_E2E_TIMEOUT_MS/, "environment docs should document live YouTube E2E timeout bounds");
assert.match(envDocs, /SHORTSENGINE_YOUTUBE_LIVE_E2E_PORT/, "environment docs should document live YouTube E2E port bounds");
assert.match(ciDocs, /npm run demo:browser:install/, "CI docs should include the Playwright browser install step");
assert.match(ciDocs, /npm run env:check/, "CI docs should include the environment readiness check");
assert.match(ciDocs, /npm run staging:check/, "CI docs should include the staging readiness check");
assert.match(ciDocs, /docs\/STAGING_DEPLOYMENT\.md/, "CI docs should link to staging deployment docs");
assert.match(ciDocs, /npm run demo:browser:ci/, "CI docs should document the CI browser E2E command");
assert.match(ciDocs, /SHORTSENGINE_BROWSER_E2E_ALLOW_SKIP/, "CI docs should make missing-runtime skips explicit");
assert.match(ciDocs, /SHORTSENGINE_BROWSER_E2E_TRACE/, "CI docs should document opt-in trace capture");
assert.match(ciDocs, /SHORTSENGINE_BROWSER_E2E_VIDEO/, "CI docs should document opt-in video capture");
assert.match(ciDocs, /SHORTSENGINE_BROWSER_E2E_RETENTION_MAX/, "CI docs should document bounded artifact retention");
assert.match(ciDocs, /playwright-artifacts/, "CI docs should document failure-only artifact location");
assert.match(ciDocs, /\.github\/workflows\/ci\.yml/, "CI docs should point to the workflow");
assert.match(ciDocs, /uploads artifacts only when the release gate fails/i, "CI docs should describe failure-only uploads");
assert.match(ciDocs, /Real cloud integration stays out of the default gate/i, "CI docs should document opt-in cloud integration");
assert.match(ciDocs, /npm run release:check/, "CI docs should include release gate verification");
assert.match(ciDocs, /npm run release:evidence/, "CI docs should include release evidence generation");
assert.match(ciDocs, /npm run release:readiness/, "CI docs should include release readiness validation");
assert.match(ciDocs, /npm run github:setup/, "CI docs should document GitHub CLI setup guidance");
assert.match(ciDocs, /npm run github:doctor/, "CI docs should document GitHub CLI doctor");
assert.match(ciDocs, /npm run remote:ci/, "CI docs should document remote CI verification");
assert.match(ciDocs, /npm run remote:ci:proof/, "CI docs should document remote CI proof");
assert.match(ciDocs, /GITHUB_NETWORK_UNAVAILABLE/, "CI docs should document GitHub doctor network failures");
assert.match(ciDocs, /gh auth status/, "CI docs should document GitHub CLI auth requirements");
assert.match(ciDocs, /does not download raw logs or artifacts/i, "CI docs should keep remote CI evidence log/artifact safe");
assert.match(ciDocs, /staging:smoke:full/, "CI docs should mention opt-in full staging smoke");
assert.match(ciDocs, /staging:smoke:cleanup/, "CI docs should mention opt-in full staging smoke cleanup");
assert.match(ciDocs, /stays out of the default gate/i, "CI docs should keep full staging smoke out of the default gate");
assert.match(ciDocs, /npm run youtube:doctor/, "CI docs should document the no-network YouTube doctor");
assert.match(ciDocs, /npm run ocr:doctor/, "CI docs should document the OCR doctor");
assert.match(ciDocs, /npm run ocr:smoke/, "CI docs should document the OCR smoke proof");
assert.match(ciDocs, /npm run ocr:qa:review/, "CI docs should document the OCR QA review proof");
assert.match(ciDocs, /ocr-qa-review-latest\.json/, "CI docs should document the OCR QA review latest report");
assert.match(ciDocs, /support-only OCR calibration/i, "CI docs should keep OCR QA review calibration support-only");
assert.match(ciDocs, /npm run youtube:smoke/, "CI docs should document the opt-in YouTube smoke command");
assert.match(ciDocs, /YouTube smoke stays out of the default gate/i, "CI docs should keep real YouTube smoke out of default CI");
assert.match(ciDocs, /docs\/YOUTUBE_INGEST_MANUAL_SMOKE\.md/, "CI docs should link to the manual YouTube ingest smoke guide");
assert.match(releaseDocs, /Branch Protection Checklist/, "release docs should include branch protection guidance");
assert.match(releaseDocs, /npm run env:check/, "release docs should include environment readiness checks");
assert.match(releaseDocs, /npm run staging:check/, "release docs should include staging readiness checks");
assert.match(releaseDocs, /npm run ocr:doctor/, "release docs should include OCR readiness checks");
assert.match(releaseDocs, /npm run ocr:smoke/, "release docs should include OCR smoke proof");
assert.match(releaseDocs, /npm run ocr:qa:review/, "release docs should include OCR QA review proof");
assert.match(releaseDocs, /ocr-qa-review-latest\.json/, "release docs should document the OCR QA review latest report");
assert.match(releaseDocs, /cannot confirm a goal by itself/i, "release docs should keep OCR QA review support-only");
assert.match(releaseDocs, /npm run render:check/, "release docs should include Render staging checks");
assert.match(releaseDocs, /\.github\/workflows\/staging\.yml/, "release docs should mention the staging workflow");
assert.match(releaseDocs, /staging:smoke:full/, "release docs should document the opt-in full staging smoke command");
assert.match(releaseDocs, /staging:smoke:cleanup/, "release docs should document the opt-in full staging smoke cleanup command");
assert.match(releaseDocs, /npm run release:check/, "release docs should explain local release checks");
assert.match(releaseDocs, /release\/results\/latest\.json/, "release docs should explain release evidence output");
assert.match(releaseDocs, /npm run release:readiness/, "release docs should explain release readiness checks");
assert.match(releaseDocs, /npm run github:setup/, "release docs should explain GitHub CLI setup guidance");
assert.match(releaseDocs, /npm run github:doctor/, "release docs should explain GitHub CLI readiness");
assert.match(releaseDocs, /npm run remote:ci/, "release docs should explain remote CI verification");
assert.match(releaseDocs, /npm run remote:ci:proof/, "release docs should explain remote CI proof generation");
assert.match(releaseDocs, /GITHUB_NETWORK_UNAVAILABLE/, "release docs should explain GitHub network failure guidance");
assert.match(releaseDocs, /remote-ci-latest\.json/, "release docs should document the remote CI proof latest report");
assert.match(releaseDocs, /fix-forward/i, "release docs should explain fix-forward after remote CI failure");
assert.match(releaseDocs, /does not download raw logs or artifacts/i, "release docs should keep remote CI logs/artifacts opt-in");
assert.match(releaseDocs, /Real cloud integration remains opt-in/i, "release docs should keep cloud integration opt-in");

for (const command of [
  "npm run env:check",
  "npm run staging:check",
  "npm run youtube:doctor",
  "npm run ocr:doctor",
  "npm run lint",
  "npm run build",
  "npm test",
  "npm run eval",
  "npm run eval:reference",
  "npm run brain:health",
  "npm run demo:fixture",
  "npm run ocr:smoke",
  "npm run ocr:qa:review",
  "npm run demo:smoke",
  "npm run demo:browser",
  "npm run demo:browser:ci",
  "npm run ci:reports",
  "npm run release:check",
]) {
  assert.match(githubWorkflow, new RegExp(command.replace(/[.*+?^${}()|[\]\\]/g, "\\$&")), `CI workflow should run ${command}`);
}
assert.doesNotMatch(githubWorkflow, /staging:smoke:full/, "CI workflow should not run full staging smoke by default");
assert.doesNotMatch(githubWorkflow, /staging:smoke:cleanup/, "CI workflow should not run full staging smoke cleanup by default");
assert.match(githubWorkflow, /pull_request:/, "CI workflow should run on pull requests");
assert.match(githubWorkflow, /push:[\s\S]*branches:[\s\S]*main[\s\S]*master/, "CI workflow should run on main/master pushes");
assert.match(githubWorkflow, /node-version:\s*"?(?:18|20|22)"?/, "CI workflow should use Node.js 18 or newer");
assert.match(githubWorkflow, /cache:\s*npm/, "CI workflow should cache npm dependencies safely");
assert.match(githubWorkflow, /~\/\.cache\/ms-playwright/, "CI workflow should cache Playwright browser binaries");
assert.match(githubWorkflow, /npm ci/, "CI workflow should use npm ci when a lockfile exists");
assert.match(githubWorkflow, /npm install/, "CI workflow should retain npm install fallback when no lockfile exists");
assert.match(githubWorkflow, /npm run demo:browser:install/, "CI workflow should install Playwright Chromium");
assert.match(githubWorkflow, /apt-get\s+install[\s\S]*\bffmpeg\b/, "CI workflow should install FFmpeg tools before runtime verification");
assert.match(githubWorkflow, /ffmpeg\s+-version[\s\S]*ffprobe\s+-version/, "CI workflow should verify FFmpeg and FFprobe availability");
assert.doesNotMatch(githubWorkflow, /MATCHCUTS_PERSISTENCE_ADAPTER\s*:\s*sqlite/, "CI workflow should not force sqlite globally because tests verify local defaults");
assert.match(githubWorkflow, /uses:\s*actions\/upload-artifact@v4/, "CI workflow should use upload-artifact for failure diagnostics");
assert.match(githubWorkflow, /if:\s*failure\(\)/, "CI workflow should upload artifacts only on failure");
assert.match(githubWorkflow, /demo\/results\/latest\.json/, "CI workflow should upload API smoke latest report on failure");
assert.match(githubWorkflow, /demo\/results\/ocr-latest\.json/, "CI workflow should upload OCR smoke latest report on failure");
assert.match(githubWorkflow, /demo\/results\/ocr-qa-review-latest\.json/, "CI workflow should upload OCR QA review latest report on failure");
assert.doesNotMatch(githubWorkflow, /demo\/results\/ocr-artifacts\//, "CI workflow should not upload OCR QA crop artifacts by default");
assert.match(githubWorkflow, /demo\/results\/browser-latest\.json/, "CI workflow should upload browser smoke latest report on failure");
assert.match(githubWorkflow, /demo\/results\/playwright-latest\.json/, "CI workflow should upload Playwright latest report on failure");
assert.match(githubWorkflow, /demo\/results\/playwright-artifacts\//, "CI workflow should upload failure-only Playwright artifacts");
assert.match(githubWorkflow, /eval\/results\/latest\.json/, "CI workflow should upload eval latest report on failure");
assert.match(githubWorkflow, /eval\/results\/reference-latest\.json/, "CI workflow should upload reference review latest report on failure");
assert.doesNotMatch(githubWorkflow, /SHORTSENGINE_BROWSER_E2E_ALLOW_SKIP/, "release gate must not skip missing Playwright runtime");
assert.doesNotMatch(githubWorkflow, /integration:cloud|MATCHCUTS_RUN_REAL_CLOUD_TESTS/, "release gate must not run real cloud integration by default");
assert.doesNotMatch(githubWorkflow, /node_modules|data\/(?:uploads|renders|db|jobs|artifacts)|var\/|\.env|secrets?/i, "CI workflow must not upload unsafe local state");
assert.doesNotMatch(githubWorkflow, /demo\/results\/\*\*|demo\/results\/\*\.json|eval\/results\/\*\.json/, "CI workflow should avoid broad report globs");

assert.match(stagingDocs, /GitHub Environment `staging`/, "staging docs should explain the protected GitHub Environment");
assert.match(stagingDocs, /SHORTSENGINE_STAGING_DEPLOY_TOKEN/, "staging docs should document the provider-neutral deploy credential");
assert.match(stagingDocs, /SHORTSENGINE_STAGING_SERVICE_ID/, "staging docs should document the Render service id");
assert.match(stagingDocs, /npm run staging:check/, "staging docs should document readiness command");
assert.match(stagingDocs, /npm run render:check/, "staging docs should document Render configuration readiness");
assert.match(stagingDocs, /npm run render:manual/, "staging docs should document the live Render manual checklist");
assert.match(stagingDocs, /npm run render:proof/, "staging docs should document the no-network proof");
assert.match(stagingDocs, /npm run staging:deploy/, "staging docs should document provider-specific staging deploy");
assert.match(stagingDocs, /npm run staging:smoke/, "staging docs should document deployed smoke command");
assert.match(stagingDocs, /npm run staging:smoke:full/, "staging docs should document opt-in full staging smoke");
assert.match(stagingDocs, /npm run staging:smoke:cleanup/, "staging docs should document opt-in full staging smoke cleanup");
assert.match(stagingDocs, /SHORTSENGINE_STAGING_FULL_SMOKE=1/, "staging docs should document the full smoke opt-in flag");
assert.match(stagingDocs, /SHORTSENGINE_STAGING_FULL_SMOKE_CLEANUP=1/, "staging docs should document explicit full smoke cleanup deletion");
assert.match(stagingDocs, /dry-run/i, "staging docs should document cleanup dry-run behavior");
assert.match(stagingDocs, /upload.*generate.*job.*export.*download/is, "staging docs should document the full upload/render/download smoke flow");
assert.match(stagingDocs, /Build command:[\s\S]*npm ci/, "staging docs should document Render build command");
assert.match(stagingDocs, /Start command:[\s\S]*npm start/, "staging docs should document Render start command");
assert.match(stagingDocs, /Health check path:[\s\S]*\/health/, "staging docs should document Render health check path");
assert.match(stagingDocs, /ephemeral/i, "staging docs should document Render ephemeral storage limitation");
assert.match(stagingDocs, /manual/i, "staging docs should document manual staging workflow dispatch");
assert.match(stagingDocs, /Auto deploy/i, "staging docs should document Render auto deploy recommendation");
assert.match(stagingDocs, /workflow status/i, "staging docs should document workflow evidence to inspect");
assert.match(stagingDocs, /provider `none`/i, "staging docs should document rollback to provider none");
assert.match(stagingDocs, /does not upload videos/i, "staging docs should keep deployed smoke health-only");
assert.match(stagingDocs, /private-network|link-local/i, "staging docs should document private network URL rejection");
assert.match(stagingDocs, /bounded health response size/i, "staging docs should document bounded health payloads");
assert.match(stagingDocs, /bounded deploy summary/i, "staging docs should document bounded Render deploy summaries");
assert.match(stagingDocs, /STAGING_RENDER_DEPLOY_JSON_INVALID/, "staging docs should document invalid Render deploy JSON failures");
assert.doesNotMatch(stagingDocs, /sk-[A-Za-z0-9_-]{20,}|AKIA[A-Z0-9]{12,}|Bearer\s+[A-Za-z0-9._-]{10,}/, "staging docs must not contain real-looking secrets");

assert.match(stagingWorkflow, /workflow_dispatch:/, "staging workflow should support manual dispatch");
assert.match(stagingWorkflow, /workflow_run:[\s\S]*ShortsEngine CI/, "staging workflow should run after successful CI");
assert.match(stagingWorkflow, /environment:[\s\S]*name:\s*staging/, "staging workflow should use GitHub Environment staging");
assert.match(stagingWorkflow, /npm run env:check/, "staging workflow should run env readiness");
assert.match(stagingWorkflow, /npm run staging:check/, "staging workflow should run staging readiness");
assert.match(stagingWorkflow, /npm run render:check/, "staging workflow should run Render staging configuration readiness");
assert.match(stagingWorkflow, /npm run staging:deploy/, "staging workflow should run provider-specific staging deploy");
assert.match(stagingWorkflow, /npm run staging:smoke/, "staging workflow should include deployed smoke");
assert.match(stagingWorkflow, /Provider-specific staging deploy/, "staging workflow should name the provider-specific deploy step");
assert.match(stagingWorkflow, /SHORTSENGINE_STAGING_SERVICE_ID/, "staging workflow should expose the Render service id variable");
assert.doesNotMatch(stagingWorkflow, /SHORTSENGINE_BROWSER_E2E_ALLOW_SKIP/, "staging workflow must not skip required browser/runtime checks");
assert.doesNotMatch(stagingWorkflow, /integration:cloud|MATCHCUTS_RUN_REAL_CLOUD_TESTS/, "staging workflow must not run real cloud integration by default");
assert.doesNotMatch(stagingWorkflow, /uses:\s*actions\/upload-artifact@v4/, "staging workflow should not upload artifacts by default");
assert.doesNotMatch(stagingWorkflow, /node_modules|data\/(?:uploads|renders|db|jobs|artifacts)|var\/|\.env|AKIA[A-Z0-9]{12,}|sk-[A-Za-z0-9_-]{20,}|Bearer\s+[A-Za-z0-9._-]{10,}/i, "staging workflow must not include unsafe local state or hardcoded secrets");

assert.match(css, /\[hidden\]\s*{[^}]*display:\s*none\s*!important/s, "hidden controls must not be overridden by display styles");
assert.match(css, /button:disabled/, "disabled states should be styled");
assert.match(css, /prefers-reduced-motion/, "reduced motion support should remain present");
assert.match(css, /\.error-panel/, "error panel should be styled");
assert.match(css, /\.reason-chip/, "candidate reason chips should be styled");

console.log("Static lint checks passed");
