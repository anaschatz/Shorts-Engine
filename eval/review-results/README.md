# Real Video Review Results

`npm run review:compare` and `npm run review:summary` write local JSON reports here.

The JSON reports are ignored by git because they can reference local operator media samples. Keep committed documentation here, but do not commit generated reports, videos, screenshots, storage keys, raw logs, provider output, or absolute local paths.
